# Socrata API token (optional, increases rate limits)
SOCRATA_APP_TOKEN=

# PostgreSQL connection (only needed if using postgres instead of DuckDB)
# DATABASE_URL=postgresql://user:password@host:5432/parcl
